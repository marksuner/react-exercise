var React = require('React');

var DashboardContent = React.createClass({
    render : function() {
        return (
            <h1>Dashboard</h1>
        );
    }
});

module.exports = DashboardContent;